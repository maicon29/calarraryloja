<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>Calarraryloja</title>
<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@600;700&family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet">
<style>
*{box-sizing:border-box;margin:0;padding:0}
:root{--ink:#17130f;--paper:#f8f5f0;--white:#fff;--terra:#bd5a3c;--sand:#ece3d7;--line:#e4dcd0;--muted:#8a8274; --green:#3d6b50}
body{font-family:Inter,system-ui,sans-serif;background:var(--paper);color:var(--ink);-webkit-font-smoothing:antialiased}
h1,h2,h3,.serif{font-family:'Playfair Display',Georgia,serif;font-weight:600}
button{font:inherit;color:inherit;background:none;border:0;cursor:pointer}
a{color:inherit;text-decoration:none}
.wrap{width:min(1140px,92vw);margin:0 auto}
.eyebrow{font-size:.7rem;letter-spacing:.2em;text-transform:uppercase;font-weight:600;color:var(--terra)}

/* header */
header{position:sticky;top:0;z-index:50;background:rgba(248,245,240,.9);backdrop-filter:blur(12px);border-bottom:1px solid var(--line)}
.nav{display:flex;align-items:center;gap:1.2rem;padding:1rem 0}
.logo{font-family:'Playfair Display',serif;font-size:1.4rem;font-weight:700}
.logo em{font-style:normal;color:var(--terra)}
nav.links{display:flex;gap:1.3rem;font-size:.86rem;font-weight:500}
nav.links a:hover{color:var(--terra)}
.spacer{margin-left:auto}
.search{display:flex;align-items:center;gap:.5rem;background:var(--white);border:1px solid var(--line);border-radius:999px;padding:.5rem .9rem;width:210px}
.search input{border:0;outline:0;background:none;font:inherit;font-size:.85rem;width:100%}
.cartbtn{display:flex;align-items:center;gap:.5rem;background:var(--ink);color:#fff;border-radius:999px;padding:.6rem 1.1rem;font-size:.82rem;font-weight:600}
.cartbtn b{background:var(--terra);border-radius:999px;min-width:20px;height:20px;display:grid;place-items:center;font-size:.68rem;padding:0 5px}
@media(max-width:820px){nav.links,.search{display:none}}

/* hero */
.hero{padding:4rem 0;display:grid;grid-template-columns:1.1fr .9fr;gap:3rem;align-items:center}
.hero h1{font-size:clamp(2.2rem,4.6vw,3.6rem);line-height:1.05;margin:.9rem 0 1.1rem}
.hero h1 em{font-style:italic;color:var(--terra)}
.hero p{color:#3b342d;font-weight:300;line-height:1.7;max-width:44ch}
.btn{display:inline-flex;align-items:center;gap:.5rem;padding:.9rem 1.6rem;border-radius:999px;font-size:.85rem;font-weight:600;transition:.25s;border:1px solid transparent}
.btn-dark{background:var(--ink);color:#fff}
.btn-dark:hover{background:var(--terra)}
.btn-line{border-color:var(--ink)}
.btn-line:hover{background:var(--ink);color:#fff}
.btn-terra{background:var(--terra);color:#fff}
.btn-terra:hover{background:#9c4530}
.hero .art{border-radius:200px 200px 20px 20px;aspect-ratio:3/4;display:grid;place-items:center}
.hero .art svg{width:50%}
@media(max-width:820px){.hero{grid-template-columns:1fr;gap:2rem;padding:2.4rem 0}.hero .art{order:-1;max-width:280px;margin:0 auto}}

/* shop */
section.shop{padding:1rem 0 5rem}
.head{text-align:center;margin-bottom:2rem}
.head h2{font-size:clamp(1.7rem,3.4vw,2.4rem);margin-top:.4rem}
.pills{display:flex;gap:.5rem;flex-wrap:wrap;justify-content:center;margin:1.6rem 0 2.2rem}
.pill{border:1px solid var(--line);background:var(--white);border-radius:999px;padding:.5rem 1.1rem;font-size:.83rem;font-weight:500;transition:.2s}
.pill:hover{border-color:var(--ink)}
.pill.on{background:var(--ink);color:#fff;border-color:var(--ink)}
.grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(230px,1fr));gap:1.8rem 1.4rem}
.card{display:flex;flex-direction:column}
.thumb{border-radius:14px;aspect-ratio:3/4;display:grid;place-items:center;overflow:hidden;position:relative;transition:.3s}
.card:hover .thumb{transform:translateY(-4px);box-shadow:0 18px 36px -20px rgba(23,19,15,.4)}
.thumb svg{width:60%;transition:.4s}
.card:hover .thumb svg{transform:scale(1.06)}
.flag{position:absolute;top:.7rem;left:.7rem;background:var(--terra);color:#fff;font-size:.62rem;font-weight:700;letter-spacing:.08em;text-transform:uppercase;padding:.28rem .6rem;border-radius:999px}
.card h3{font-family:Inter,sans-serif;font-size:.92rem;font-weight:500;margin-top:.85rem}
.card .cat{font-size:.68rem;letter-spacing:.13em;text-transform:uppercase;color:var(--muted);font-weight:600}
.card .row{display:flex;align-items:center;justify-content:space-between;margin-top:.4rem}
.card .price b{font-size:1rem}
.card .price s{font-size:.76rem;color:var(--muted);margin-left:.4rem}
.add{background:var(--sand);border-radius:999px;padding:.45rem .9rem;font-size:.76rem;font-weight:600}
.add:hover{background:var(--ink);color:#fff}
.empty{grid-column:1/-1;text-align:center;padding:3.5rem 0;color:var(--muted)}
footer{background:#12100d;color:#c6bdb1;padding:3rem 0;font-size:.85rem}
footer .top{display:flex;justify-content:space-between;gap:2rem;flex-wrap:wrap;padding-bottom:1.6rem;border-bottom:1px solid rgba(255,255,255,.1)}
footer .logo{color:#fff}
footer p{max-width:38ch;line-height:1.8;font-weight:300;opacity:.75;margin-top:.7rem}
footer .cols{display:flex;gap:2.5rem;flex-wrap:wrap}
footer h5{color:#fff;font-size:.72rem;letter-spacing:.14em;text-transform:uppercase;margin-bottom:.8rem}
footer li{list-style:none;margin-bottom:.5rem;opacity:.8}
footer .bot{padding-top:1.4rem;font-size:.76rem;opacity:.55}

/* drawer */
.backdrop{position:fixed;inset:0;background:rgba(20,17,14,.5);opacity:0;pointer-events:none;transition:.3s;z-index:70}
.backdrop.on{opacity:1;pointer-events:auto}
.drawer{position:fixed;top:0;right:0;height:100%;width:min(400px,100%);background:var(--paper);z-index:80;display:flex;flex-direction:column;transform:translateX(102%);transition:.35s cubic-bezier(.3,.8,.3,1)}
.drawer.open{transform:none}
.dh{display:flex;justify-content:space-between;align-items:center;padding:1.2rem;border-bottom:1px solid var(--line)}
.db{flex:1;overflow:auto;padding:1rem 1.2rem}
.df{border-top:1px solid var(--line);padding:1.2rem;background:var(--white)}
.x{width:34px;height:34px;border-radius:999px;display:grid;place-items:center}
.x:hover{background:var(--sand)}
.item{display:grid;grid-template-columns:70px 1fr;gap:.9rem;padding:.9rem 0;border-bottom:1px solid var(--line)}
.item .mini{border-radius:10px;aspect-ratio:3/4;display:grid;place-items:center}
.item .mini svg{width:65%}
.item h4{font-family:Inter,sans-serif;font-size:.87rem;font-weight:600}
.item small{color:var(--muted);font-size:.74rem;display:block;margin:.15rem 0 .45rem}
.qty{display:inline-flex;align-items:center;border:1px solid var(--line);border-radius:999px;background:#fff}
.qty button{width:26px;height:26px;font-size:.95rem}
.qty span{min-width:22px;text-align:center;font-size:.82rem;font-weight:600}
.item .right{text-align:right;display:flex;flex-direction:column;justify-content:space-between;font-size:.88rem}
.rm{font-size:.72rem;color:var(--muted);text-decoration:underline}
.tot{display:flex;justify-content:space-between;font-size:.85rem;padding:.25rem 0}
.tot.big{font-family:'Playfair Display',serif;font-size:1.15rem;font-weight:700;border-top:1px solid var(--line);margin-top:.6rem;padding-top:.7rem}
.note{font-size:.75rem;color:var(--green);text-align:center;margin-top:.5rem}
.empty-c{text-align:center;padding:3rem 0;color:var(--muted)}

/* modal */
.modal{position:fixed;inset:0;z-index:85;display:grid;place-items:center;padding:1rem;background:rgba(20,17,14,.5);opacity:0;pointer-events:none;transition:.3s}
.modal.on{opacity:1;pointer-events:auto}
.sheet{background:var(--paper);border-radius:18px;width:min(760px,100%);max-height:92vh;overflow:auto;display:grid;grid-template-columns:1fr 1fr;transform:translateY(12px);transition:.3s}
.modal.on .sheet{transform:none}
.sheet .big{display:grid;place-items:center;min-height:380px;position:relative}
.sheet .big svg{width:55%}
.sheet .info{padding:1.8rem}
.sheet h2{font-size:1.6rem;margin:.5rem 0}
.sheet p.d{font-size:.87rem;line-height:1.7;color:#3b342d;font-weight:300;margin:1rem 0}
.opt{margin-top:1.2rem}
.opt .l{font-size:.7rem;letter-spacing:.13em;text-transform:uppercase;color:var(--muted);font-weight:700;margin-bottom:.5rem}
.swatch{display:flex;gap:.5rem}
.swatch button{width:28px;height:28px;border-radius:999px;border:2px solid transparent;box-shadow:inset 0 0 0 1px rgba(0,0,0,.15)}
.swatch button.on{border-color:var(--ink)}
.sizes{display:flex;gap:.45rem;flex-wrap:wrap}
.sizes button{min-width:44px;padding:.5rem .6rem;border:1px solid var(--line);border-radius:8px;background:#fff;font-size:.82rem;font-weight:600}
.sizes button.on{background:var(--ink);color:#fff;border-color:var(--ink)}
.warn{font-size:.74rem;color:var(--terra);margin-top:.5rem}
.sheet .btns{display:flex;gap:.6rem;margin-top:1.6rem}
.sheet .btns .btn{flex:1;justify-content:center}
@media(max-width:760px){.sheet{grid-template-columns:1fr}.sheet .big{min-height:260px}}

/* checkout */
.checkout{position:fixed;inset:0;z-index:90;background:var(--paper);overflow:auto;transform:translateY(100%);transition:.4s cubic-bezier(.3,.8,.3,1)}
.checkout.on{transform:none}
.coh{position:sticky;top:0;background:rgba(248,245,240,.92);backdrop-filter:blur(12px);border-bottom:1px solid var(--line);z-index:2}
.coh .in{display:flex;align-items:center;gap:1rem;padding:1rem 0}
.cog{display:grid;grid-template-columns:1.4fr .8fr;gap:2.4rem;padding:2.4rem 0 4rem;align-items:start}
.card-box{background:#fff;border:1px solid var(--line);border-radius:14px;padding:1.5rem}
.card-box h3{font-size:1.2rem;margin-bottom:1rem}
.field{margin-bottom:1rem}
.field label{display:block;font-size:.76rem;font-weight:600;margin-bottom:.35rem}
.field input,.field select{width:100%;border:1px solid var(--line);border-radius:10px;padding:.75rem 1rem;font:inherit;font-size:.88rem;background:#fff;outline:0}
.field input:focus{border-color:var(--ink)}
.field input.err{border-color:var(--terra)}
.g2{display:grid;grid-template-columns:1fr 1fr;gap:.9rem}
.g3{display:grid;grid-template-columns:1fr 1fr 1fr;gap:.9rem}
@media(max-width:640px){.g2,.g3{grid-template-columns:1fr}}
.pay{display:flex;align-items:center;gap:.7rem;border:1px solid var(--line);border-radius:12px;padding:.9rem;margin-bottom:.6rem;background:#fff;cursor:pointer}
.pay.on{border-color:var(--ink);box-shadow:0 0 0 3px rgba(23,19,15,.06)}
.radio{width:17px;height:17px;border-radius:999px;border:2px solid var(--line)}
.pay.on .radio{border-color:var(--terra);background:radial-gradient(circle,var(--terra) 45%,transparent 46%)}
.pay b{font-size:.87rem;display:block}
.pay small{font-size:.74rem;color:var(--muted)}
.sum{background:#fff;border:1px solid var(--line);border-radius:14px;padding:1.4rem;position:sticky;top:92px}
.sum h4{font-family:Inter,sans-serif;font-size:.75rem;letter-spacing:.14em;text-transform:uppercase;color:var(--muted);margin-bottom:1rem}
.sitem{display:flex;gap:.7rem;font-size:.83rem;padding:.55rem 0;border-bottom:1px solid var(--line)}
.sitem .mini{width:46px;aspect-ratio:3/4;border-radius:8px;display:grid;place-items:center;flex:none}
.sitem b{margin-left:auto;white-space:nowrap}
.sitem small{color:var(--muted);display:block;font-size:.72rem}
@media(max-width:820px){.cog{grid-template-columns:1fr}.sum{position:static}}
.done{text-align:center;max-width:560px;margin:0 auto;padding:3.5rem 0}
.done .ok{width:70px;height:70px;border-radius:999px;background:rgba(61,107,80,.12);color:var(--green);display:grid;place-items:center;margin:0 auto 1.4rem}
.done h2{font-size:1.9rem}
.done p{color:var(--muted);margin-top:.7rem;line-height:1.7;font-size:.9rem}
.done .box{background:#fff;border:1px solid var(--line);border-radius:14px;padding:1.3rem;margin:1.8rem 0;text-align:left;font-size:.85rem}
.done .box div{display:flex;justify-content:space-between;padding:.35rem 0}

/* toast */
.toasts{position:fixed;bottom:1.2rem;left:50%;transform:translateX(-50%);z-index:120;display:grid;gap:.5rem;width:min(340px,92vw)}
.toast{background:var(--ink);color:#fff;padding:.8rem 1rem;border-radius:12px;font-size:.83rem;text-align:center;animation:up .3s}
.toast.ok{background:var(--green)}
.toast.warn{background:#9c4530}
@keyframes up{from{opacity:0;transform:translateY(12px)}to{opacity:1;transform:none}}
</style>
</head>
<body>

<header>
  <div class="wrap nav">
    <a href="#" class="logo">Calarrary</a>
    <nav class="links">
      <a href="#loja" data-cat="Vestidos">Vestidos</a>
      <a href="#loja" data-cat="Alfaiataria">Alfaiataria</a>
      <a href="#loja" data-cat="Casacos">Casacos</a>
    </nav>
    <div class="spacer"></div>
    <form class="search" id="searchForm">
      <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="11" cy="11" r="7"/><path d="m20 20-3.5-3.5"/></svg>
      <input id="q" type="search" placeholder="Buscar peça...">
    </form>
    <button class="cartbtn" id="openCart">
      <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"><path d="M6 8h12l1 12H5L6 8z"/><path d="M9 8V6a3 3 0 0 1 6 0v2"/></svg>
      Sacola <b id="badge" hidden>0</b>
    </button>
  </div>
</header>

<section class="wrap hero">
  <div>
    <p class="eyebrow">Nova coleção</p>
    <h1>Roupas simples de usar, fáceis de <em>amar</em>.</h1>
    <p>Peças atemporais em algodão, linho e alfaiataria leve. Produzidas em pequenos lotes no nosso ateliê em São Paulo.</p>
    <div style="margin-top:1.8rem"><a href="#loja" class="btn btn-dark">Ver a coleção</a></div>
  </div>
  <div class="art" id="heroArt"></div>
</section>

<section class="shop wrap" id="loja">
  <div class="head">
    <p class="eyebrow">Vitrine</p>
    <h2 class="serif">Peças da estação</h2>
  </div>
  <div class="pills" id="pills"></div>
  <div class="grid" id="grid"></div>
</section>

<footer>
  <div class="wrap">
    <div class="top">
      <div>
        <a href="#" class="logo">Calarrary <em>Atelier</em></a>
        <p>Moda autoral brasileira feita em pequenos lotes na para todo São Paulo.</p>
      </div>
      <div class="cols">
        <div>
          <h5>Loja</h5>
          <ul><li><a href="#loja">Novidades</a></li><li><a href="#loja">Vestidos</a></li><li><a href="#loja">Básicos</a></li><li><a href="#loja">Casacos</a></li></ul>
        </div>
        <div>
          <h5>Ajuda</h5>
          <ul><li><a href="#">Trocas e devoluções</a></li><li><a href="#">Guia de tamanhos</a></ul>
        </div>
        <div>
          <h5>Contato</h5>
          <ul><li>(11) 0000-0000</li><li>Seg a sab, 8h às 22h</li></ul>
        </div>
      </div>
    </div>
    <div class="bot">© 2026 Calarrary · Frete grátis acima de R$ 000 · Parcele em x sem juros</div>
  </div>
</footer>

<div class="backdrop" id="backdrop"></div>

<aside class="drawer" id="drawer">
  <div class="dh">
    <h3 class="serif">fale comigo</h3>
    <button class="x" id="closeCart" aria-label="Fechar">
      <svg width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M6 6l12 12M18 6 6 18"/></svg>
    </button>
  </div>
  <div class="db" id="cartBody"></div>
  <div class="df" id="cartFoot"></div>
</aside>

<div class="modal" id="modal"><div class="sheet" id="sheet"></div></div>

<div class="checkout" id="checkout">
  <div class="coh">
    <div class="wrap in">
      <span class="logo" style="font-size:1.2rem">Lume <em>Atelier</em></span>
      <span style="font-size:.82rem;color:var(--muted);margin-left:auto">Checkout seguro</span>
      <button class="x" id="closeCo" aria-label="Fechar">
        <svg width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M6 6l12 12M18 6 6 18"/></svg>
      </button>
    </div>
  </div>
  <div class="wrap" id="coBody"></div>
</div>

<div class="toasts" id="toasts"></div>

<script>
/* ---------- desenhos das peças ---------- */
const SHAPES = {
  tee:'<path d="M38 20 L22 27 L12 47 L25 55 L33 43 L33 101 L67 101 L67 43 L75 55 L88 47 L78 27 L62 20 C58 29 42 29 38 20 Z"/>',
  dress:'<path d="M35 20 L24 28 L30 49 L35 47 L24 106 L76 106 L65 47 L70 49 L76 28 L65 20 C60 30 40 30 35 20 Z"/>',
  jacket:'<path d="M36 20 L20 27 L12 53 L25 59 L31 47 L31 103 L69 103 L69 47 L75 59 L88 53 L80 27 L64 20 L50 33 Z"/>',
  pants:'<path d="M30 18 L70 18 L73 45 L64 105 L54 105 L50 53 L48 53 L44 105 L34 105 L27 45 Z"/>',
  shirt:'<path d="M38 20 L20 28 L14 51 L27 57 L33 45 L33 101 L67 101 L67 45 L73 57 L86 51 L80 28 L62 20 L50 31 Z"/>',
  hoodie:'<path d="M37 21 C37 30 63 30 63 21 L78 27 L89 49 L76 57 L70 45 L70 101 L30 101 L30 45 L24 57 L11 49 L22 27 Z"/>',
  skirt:'<path d="M31 24 L69 24 L78 97 L22 97 Z"/><rect x="29" y="17" width="42" height="8" rx="3"/>',
  coat:'<path d="M36 19 L17 28 L9 62 L22 68 L29 51 L29 113 L71 113 L71 51 L78 68 L91 62 L83 28 L64 19 L50 33 Z"/>'
};
const art=(type,tone)=>'<svg viewBox="0 0 100 120" fill="'+tone+'">'+(SHAPES[type]||SHAPES.tee)+'</svg>';

/* ---------- produtos ---------- */
const PRODUCTS=[
 {id:1,n:'Vestido Midi Solaris',c:'Vestidos',t:'dress',p:389.9,old:459.9,flag:'Novo',bg:'linear-gradient(150deg,#f0d9c8,#dcb69b)',
  cols:[{n:'Terracota',h:'#b5643f'},{n:'Areia',h:'#d8c3a5'},{n:'Preto',h:'#2f2b27'}],sz:['P','M','G','GG'],
  d:'Viscose de origem responsável, decote V e cinto removível. Caimento fluido, forro interno e bolsos laterais.'},
 {id:2,n:'Camiseta Pima Essencial',c:'Básicos',t:'tee',p:119.9,old:null,flag:'Best seller',bg:'linear-gradient(150deg,#eeeae3,#d8cfc2)',
  cols:[{n:'Off-white',h:'#f2efe9'},{n:'Preto',h:'#2b2724'},{n:'Azul',h:'#8ea3bb'}],sz:['PP','P','M','G','GG'],
  d:'Algodão pima de fio longo: não encolhe e fica mais macio a cada lavagem. Gola dupla reforçada.'},
 {id:3,n:'Blazer Nobile',c:'Alfaiataria',t:'jacket',p:649,old:749,flag:'-13%',bg:'linear-gradient(150deg,#e2ded6,#c3bdb3)',
  cols:[{n:'Grafite',h:'#403a34'},{n:'Caramelo',h:'#8b6a4c'},{n:'Musgo',h:'#7d8975'}],sz:['P','M','G','GG'],
  d:'Ombro estruturado leve e forro em cupro. Modelagem reta que não aperta os braços — feita para o dia inteiro.'},
 {id:4,n:'Calça Wide Leg Milano',c:'Calças',t:'pants',p:329.9,old:null,flag:null,bg:'linear-gradient(150deg,#e6e2dc,#c8c2b8)',
  cols:[{n:'Preto',h:'#2e2a27'},{n:'Bege',h:'#c2ac93'},{n:'Cinza',h:'#8b9099'}],sz:['P','M','G','GG'],
  d:'Cintura alta com elástico embutido nas costas, pregas frontais e caimento amplo até o tornozelo.'},
 {id:5,n:'Camisa Linho Litoral',c:'Camisas',t:'shirt',p:259.9,old:null,flag:null,bg:'linear-gradient(150deg,#e9eee6,#c7d1c4)',
  cols:[{n:'Off-white',h:'#efeade'},{n:'Sálvia',h:'#b3c2b4'},{n:'Azul céu',h:'#9db3c9'}],sz:['P','M','G','GG'],
  d:'Linho europeu lavado em pedra, que amassa menos e seca rápido. Botões em madrepérola natural.'},
 {id:6,n:'Moletom Oversized Aurora',c:'Moletons',t:'hoodie',p:299.9,old:359.9,flag:'-17%',bg:'linear-gradient(150deg,#e4dded,#c7bede)',
  cols:[{n:'Lilás',h:'#a58cc0'},{n:'Cinza',h:'#7c7770'},{n:'Preto',h:'#2f2b27'}],sz:['P','M','G','GG'],
  d:'Moletom felpado de algodão orgânico com capuz forrado e bolso canguru. Modelagem ampla e confortável.'},
 {id:7,n:'Saia Midi Plissê Vento',c:'Saias',t:'skirt',p:279.9,old:null,flag:null,bg:'linear-gradient(150deg,#f1e5d8,#d9c1a6)',
  cols:[{n:'Caramelo',h:'#c98f5e'},{n:'Areia',h:'#cbbba6'},{n:'Preto',h:'#302c28'}],sz:['P','M','G'],
  d:'Plissê permanente em poliéster reciclado, cintura elástica confortável e movimento que acompanha o corpo.'},
 {id:8,n:'Trench Coat Serra',c:'Casacos',t:'coat',p:899,old:1090,flag:'-18%',bg:'linear-gradient(150deg,#e0d8c8,#bfb198)',
  cols:[{n:'Cáqui',h:'#9c8a6b'},{n:'Preto',h:'#37332e'},{n:'Marrom',h:'#7b6754'}],sz:['P','M','G','GG'],
  d:'Gabardine impermeável com cinto, transposição dupla e forro xadrez removível. Resolve o inverno inteiro.'}
];

/* ---------- estado ---------- */
const st={cart:[],cat:'Todos',q:'',temp:null};
const brl=v=>v.toLocaleString('pt-BR',{style:'currency',currency:'BRL'});
const $=s=>document.querySelector(s);
const byId=id=>PRODUCTS.find(p=>p.id==id);
const count=()=>st.cart.reduce((a,i)=>a+i.q,0);
const sub=()=>st.cart.reduce((a,i)=>a+byId(i.id).p*i.q,0);
const FREE=299, SHIP=24.9;
const shipCost=()=>st.cart.length? (sub()>=FREE?0:SHIP) : 0;

function toast(msg,kind){
  const el=document.createElement('div');
  el.className='toast '+(kind||''); el.textContent=msg;
  $('#toasts').appendChild(el);
  setTimeout(()=>{el.style.transition='.3s';el.style.opacity='0';setTimeout(()=>el.remove(),300)},2600);
}

/* ---------- vitrine ---------- */
const CATS=['Todos',...new Set(PRODUCTS.map(p=>p.c))];
$('#pills').innerHTML=CATS.map(c=>'<button class="pill'+(c==='Todos'?' on':'')+'" data-cat="'+c+'">'+c+'</button>').join('');

function visible(){
  return PRODUCTS.filter(p=>
    (st.cat==='Todos'||p.c===st.cat) &&
    (!st.q || (p.n+p.c+p.d).toLowerCase().includes(st.q.toLowerCase()))
  );
}
function card(p){
  return '<article class="card" data-id="'+p.id+'">'+
    '<div class="thumb" style="background:'+p.bg+'">'+(p.flag?'<span class="flag">'+p.flag+'</span>':'')+art(p.t,p.cols[0].h)+'</div>'+
    '<span class="cat" style="margin-top:.7rem">'+p.c+'</span>'+
    '<h3>'+p.n+'</h3>'+
    '<div class="row"><div class="price"><b>'+brl(p.p)+'</b>'+(p.old?'<s>'+brl(p.old)+'</s>':'')+'</div>'+
    '<button class="add" data-open="'+p.id+'">Escolher</button></div>'+
  '</article>';
}
function renderGrid(){
  const list=visible();
  $('#grid').innerHTML = list.length? list.map(card).join('')
    : '<div class="empty"><h3 class="serif">Nenhuma peça encontrada</h3><p>Tente outra busca ou escolha outra categoria.</p></div>';
}
function renderHero(){ $('#heroArt').innerHTML='<div style="width:100%;display:grid;place-items:center">'+art('dress','#b5643f')+'</div>'; }
$('#heroArt').style.background='linear-gradient(150deg,#f0d9c8,#d9b79f)';

/* ---------- produto ---------- */
function openProduct(id){
  const p=byId(id);
  st.temp={id,color:0,size:null};
  $('#sheet').innerHTML=
   '<div class="big" style="background:'+p.bg+'">'+art(p.t,p.cols[0].h)+
     '<button class="x" id="closeModal" style="position:absolute;top:.7rem;right:.7rem;background:rgba(255,255,255,.85)" aria-label="Fechar">'+
     '<svg width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M6 6l12 12M18 6 6 18"/></svg></button></div>'+
   '<div class="info"><span class="cat">'+p.c+'</span>'+
     '<h2 class="serif">'+p.n+'</h2>'+
     '<div class="price"><b style="font-size:1.35rem">'+brl(p.p)+'</b>'+(p.old?'<s style="color:var(--muted);font-size:.85rem;margin-left:.5rem">'+brl(p.old)+'</s>':'')+'</div>'+
     '<p style="font-size:.78rem;color:var(--muted);margin-top:.25rem">ou 6x de '+brl(p.p/6)+' sem juros</p>'+
     '<p class="d">'+p.d+'</p>'+
     '<div class="opt"><div class="l">Cor: <span id="cn" style="text-transform:none;letter-spacing:0;color:var(--ink)">'+p.cols[0].n+'</span></div>'+
       '<div class="swatch">'+p.cols.map((c,i)=>'<button class="'+(i?'':'on')+'" data-color="'+i+'" style="background:'+c.h+'"></button>').join('')+'</div></div>'+
     '<div class="opt"><div class="l">Tamanho</div><div class="sizes">'+p.sz.map(s=>'<button data-size="'+s+'">'+s+'</button>').join('')+'</div>'+
       '<p class="warn" id="sizeWarn">Escolha um tamanho para adicionar</p></div>'+
     '<div class="btns"><button class="btn btn-dark" id="addBtn">Adicionar à sacola</button></div>'+
     '<p style="font-size:.76rem;color:var(--muted);margin-top:1rem">Frete grátis acima de R$ 299 · troca grátis em 30 dias</p>'+
   '</div>';
  $('#modal').classList.add('on');
}
function closeProduct(){ $('#modal').classList.remove('on'); }

/* ---------- sacola ---------- */
function addToCart(id,size,colorName,q){
  const k=id+'|'+size+'|'+colorName;
  const ex=st.cart.find(i=>i.k===k);
  if(ex) ex.q+=q; else st.cart.push({k,id,size,color:colorName,q});
  renderCart(); toast('Adicionado à sacola ✓','ok');
}
function renderCart(){
  const n=count();
  $('#badge').textContent=n; $('#badge').hidden=n===0;
  if(!st.cart.length){
    $('#cartBody').innerHTML='<div class="empty-c"><h3 class="serif" style="color:var(--ink)">Sua sacola está vazia</h3><p style="font-size:.85rem;margin-top:.5rem">Escolha suas peças favoritas na vitrine.</p></div>';
    $('#cartFoot').innerHTML='';
    return;
  }
  $('#cartBody').innerHTML=st.cart.map(i=>{
    const p=byId(i.id), col=p.cols.find(c=>c.n===i.color)||p.cols[0];
    return '<div class="item"><div class="mini" style="background:'+p.bg+'">'+art(p.t,col.h)+'</div>'+
      '<div><h4>'+p.n+'</h4><small>Tam '+i.size+' · '+i.color+'</small>'+
      '<span class="qty"><button data-dec="'+i.k+'">−</button><span>'+i.q+'</span><button data-inc="'+i.k+'">+</button></span> '+
      '<button class="rm" data-rm="'+i.k+'">remover</button></div>'+
      '<div class="right"><b>'+brl(p.p*i.q)+'</b></div></div>';
  }).join('');
  const s=sub(), f=shipCost();
  const falta=FREE-s;
  $('#cartFoot').innerHTML=
    (falta>0?'<p style="font-size:.78rem;color:var(--muted);margin-bottom:.7rem">Faltam <b>'+brl(falta)+'</b> para o frete grátis.</p>'
            :'<p style="font-size:.78rem;color:var(--green);margin-bottom:.7rem">Você ganhou frete grátis ✓</p>')+
    '<div class="tot"><span>Subtotal</span><span>'+brl(s)+'</span></div>'+
    '<div class="tot"><span>Frete</span><span>'+(f===0?'Grátis':brl(f))+'</span></div>'+
    '<div class="tot big"><span>Total</span><span>'+brl(s+f)+'</span></div>'+
    '<button class="btn btn-dark" id="goCheckout" style="width:100%;justify-content:center;margin-top:1rem">Finalizar compra</button>';
}
function openCart(){ $('#drawer').classList.add('open'); $('#backdrop').classList.add('on'); document.body.style.overflow='hidden'; }
function closeCart(){ $('#drawer').classList.remove('open'); $('#backdrop').classList.remove('on'); document.body.style.overflow=''; }

/* ---------- checkout ---------- */
function openCheckout(){
  if(!st.cart.length){ toast('Sua sacola está vazia','warn'); return; }
  closeCart(); $('#checkout').classList.add('on'); document.body.style.overflow='hidden';
  $('#checkout').scrollTop=0; renderCheckout();
}
function closeCheckout(){ $('#checkout').classList.remove('on'); document.body.style.overflow=''; }
function renderCheckout(){
  const s=sub(), f=shipCost();
  const items=st.cart.map(i=>{const p=byId(i.id),col=p.cols.find(c=>c.n===i.color)||p.cols[0];
    return '<div class="sitem"><div class="mini" style="background:'+p.bg+'">'+art(p.t,col.h)+'</div>'+
      '<div>'+p.n+'<small>Tam '+i.size+' · '+i.q+'x</small></div><b>'+brl(p.p*i.q)+'</b></div>';}).join('');
  $('#coBody').innerHTML=
   '<div class="cog"><div>'+
     '<div class="card-box"><h3 class="serif">Seus dados</h3>'+
       '<div class="field"><label>Nome completo *</label><input id="f_nome" placeholder="Como está no documento"></div>'+
       '<div class="g2"><div class="field"><label>E-mail *</label><input id="f_mail" type="email" placeholder="voce@email.com"></div>'+
       '<div class="field"><label>Telefone *</label><input id="f_tel" placeholder="(11) 99999-9999"></div></div>'+
     '</div>'+
     '<div class="card-box" style="margin-top:1.2rem"><h3 class="serif">Entrega</h3>'+
       '<div class="g2"><div class="field"><label>CEP *</label><input id="f_cep" placeholder="00000-000"></div>'+
       '<div class="field"><label>Endereço *</label><input id="f_end" placeholder="Rua, avenida..."></div></div>'+
       '<div class="g3"><div class="field"><label>Número *</label><input id="f_num"></div>'+
       '<div class="field"><label>Bairro</label><input id="f_bairro"></div>'+
       '<div class="field"><label>Cidade *</label><input id="f_cid"></div></div>'+
     '</div>'+
     '<div class="card-box" style="margin-top:1.2rem"><h3 class="serif">Pagamento</h3>'+
       '<label class="pay on" data-pay="pix"><span class="radio"></span><div><b>Pix</b><small>Aprovação na hora · 5% de desconto</small></div></label>'+
       '<label class="pay" data-pay="cartao"><span class="radio"></span><div><b>Cartão de crédito</b><small>Até 6x sem juros</small></div></label>'+
       '<label class="pay" data-pay="boleto"><span class="radio"></span><div><b>Boleto bancário</b><small>Vence em 2 dias úteis</small></div></label>'+
     '</div>'+
     '<div style="display:flex;gap:.7rem;margin-top:1.4rem;flex-wrap:wrap">'+
       '<button class="btn btn-line" id="backShop">Continuar comprando</button>'+
       '<button class="btn btn-terra" id="payNow">Pagar '+brl((s+f)*0.95)+' no Pix</button>'+
     '</div>'+
   '</div>'+
   '<aside class="sum"><h4>Resumo</h4>'+items+
     '<div class="tot" style="margin-top:.9rem"><span>Subtotal</span><span>'+brl(s)+'</span></div>'+
     '<div class="tot"><span>Frete</span><span>'+(f===0?'Grátis':brl(f))+'</span></div>'+
     '<div class="tot b ig big" style="font-family:\'Playfair Display\',serif;font-size:1.15rem;font-weight:700;border-top:1px solid var(--line);margin-top:.5rem;padding-top:.6rem"><span>Total</span><span>'+brl(s+f)+'</span></div>'+
     '<p class="note">6x de '+brl((s+f)/6)+' sem juros · Pix '+brl((s+f)*0.95)+'</p>'+
   '</aside></div>';
}
function placeOrder(){
  const g=s=>$('#'+s).value.trim();
  const need=[['f_nome','nome'],['f_mail','e-mail'],['f_tel','telefone'],['f_cep','CEP'],['f_end','endereço'],['f_num','número'],['f_cid','cidade']];
  let bad=false;
  need.forEach(([id])=>{ const el=$('#'+id); el.classList.remove('err');
    if(!el.value.trim()||(id==='f_mail'&&!/^\S+@\S+\.\S+$/.test(el.value))){ el.classList.add('err'); bad=true; } });
  if(bad){ toast('Confira os campos destacados','warn'); return; }
  const s=sub(), f=shipCost(), total=st.cart.length? (s+f)*0.95 : 0;
  const order={id:'LM-'+Math.floor(100000+Math.random()*899999), total, mail:g('f_mail')};
  st.cart=[]; renderCart();
  $('#coBody').innerHTML='<div class="done">'+
    '<div class="ok"><svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="m5 13 4.5 4.5L19 7"/></svg></div>'+
    '<h2 class="serif">Pedido confirmado!</h2>'+
    '<p>Obrigada por vestir Lume Atelier. Enviamos a confirmação para <b>'+order.mail+'</b>.</p>'+
    '<div class="box"><div><span>Pedido</span><b>'+order.id+'</b></div>'+
      '<div><span>Entrega</span><b>5 a 8 dias úteis</b></div>'+
      '<div><span>Pagamento</span><b>Pix · 5% off</b></div>'+
      '<div style="border-top:1px solid var(--line);margin-top:.5rem;padding-top:.6rem"><span>Total pago</span><b>'+brl(order.total)+'</b></div></div>'+
    '<button class="btn btn-dark" id="doneBtn">Voltar para a loja</button></div>';
  toast('Pedido confirmado! 🎉','ok');
}

/* ---------- eventos ---------- */
document.addEventListener('click',e=>{
  const cat=e.target.closest('[data-cat]');
  if(cat){ st.cat=cat.dataset.cat;
    document.querySelectorAll('.pill').forEach(p=>p.classList.toggle('on',p.dataset.cat===st.cat));
    renderGrid(); document.getElementById('loja').scrollIntoView({behavior:'smooth'}); return; }

  const op=e.target.closest('[data-open]');
  if(op){ openProduct(op.dataset.open); return; }

  const c=e.target.closest('[data-color]');
  if(c){ st.temp.color=+c.dataset.color; const p=byId(st.temp.id);
    document.querySelectorAll('.swatch button').forEach(b=>b.classList.remove('on')); c.classList.add('on');
    $('#cn').textContent=p.cols[st.temp.color].n;
    const g=$('#sheet .big svg'); g.setAttribute('fill',p.cols[st.temp.color].h); return; }

  const sz=e.target.closest('[data-size]');
  if(sz){ st.temp.size=sz.dataset.size;
    document.querySelectorAll('.sizes button').forEach(b=>b.classList.remove('on')); sz.classList.add('on');
    $('#sizeWarn').style.color='var(--green)'; $('#sizeWarn').textContent='Tamanho '+st.temp.size+' disponível'; return; }

  const inc=e.target.closest('[data-inc]'), dec=e.target.closest('[data-dec]'), rm=e.target.closest('[data-rm]');
  if(inc||dec||rm){
    const k=(inc||dec||rm).dataset.inc||(inc||dec||rm).dataset.dec||(inc||dec||rm).dataset.rm;
    const it=st.cart.find(i=>i.k===k);
    if(rm) st.cart=st.cart.filter(i=>i!==it);
    else { it.q += inc?1:-1; if(it.q<=0) st.cart=st.cart.filter(i=>i!==it); }
    renderCart(); return;
  }

  if(e.target.closest('#addBtn')){
    if(!st.temp.size){ toast('Escolha um tamanho primeiro','warn'); return; }
    const p=byId(st.temp.id);
    addToCart(p.id,st.temp.size,p.cols[st.temp.color].n,1); closeProduct(); openCart(); return; }

  if(e.target.closest('#closeModal')){ closeProduct(); return; }
  if(e.target.id==='modal'){ closeProduct(); return; }
  if(e.target.closest('#openCart')){ openCart(); return; }
  if(e.target.closest('#closeCart')){ closeCart(); return; }
  if(e.target.id==='backdrop'){ closeCart(); return; }
  if(e.target.closest('#goCheckout')){ openCheckout(); return; }
  if(e.target.closest('#closeCo')){ closeCheckout(); return; }
  if(e.target.closest('#backShop')){ closeCheckout(); document.getElementById('loja').scrollIntoView({behavior:'smooth'}); return; }
  if(e.target.closest('#doneBtn')){ closeCheckout(); renderGrid(); return; }
  const pay=e.target.closest('.pay');
  if(pay){ document.querySelectorAll('.pay').forEach(p=>p.classList.remove('on')); pay.classList.add('on'); return; }
  if(e.target.closest('#payNow')){ placeOrder(); return; }
});
document.addEventListener('keydown',e=>{ if(e.key==='Escape'){ closeProduct(); closeCart(); } });
$('#searchForm').addEventListener('submit',e=>e.preventDefault());
let tmr;
$('#q').addEventListener('input',e=>{ clearTimeout(tmr); tmr=setTimeout(()=>{ st.q=e.target.value.trim(); renderGrid(); },250); });

/* ---------- start ----------. */
renderHero(); renderGrid(); renderCart();
</script>
</body>
</html>